<%= form_for @recipe do |f| %>
    <%= f.label :name %>:
    <%= f.text_field :name %> <br><br>
    <%= f.label :ingredient_ids, "Ingredients" %>: <br>
    <%= f.collection_check_boxes :ingredient_ids, @ingredients, :id, :name, {id: :name} %>
    <br><br>
    <%= f.submit %>
<% end %>